<h2>新規会員登録</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "public/shared/error_messages", resource: resource %>
  
 <%= render 'public/shared/form' %>

<h2>既に登録済みの方</h2>
<%= render "public/shared/links" %>
