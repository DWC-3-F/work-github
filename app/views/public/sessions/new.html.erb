
<h2>ログイン</h2>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<p>会員の方はこちらからログイン</p>
<table>
  <tr>
    <td>メールアドレス</td>
    <td><%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@wxsample.com" %></td>
  </tr>
  <tr>
    <td>パスワード</td>
    <td><%= f.password_field :password, autocomplete: "current-password" %></td>
  </tr>
  <tr>
    <td></td>
    <td><%= f.submit "ログイン" %></td>
  </tr>
</table>
<% end %>
<p>会員登録がお済みでない方</p>
<%= render "public/shared/links" %>