<h2>ジャンル一覧・追加</h2>
<%= render 'form', genre: @genre %>

<table>
  <thead>
    <tr>
      <th>ジャンル</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
    <tr>
      <td>
        <%= genre.name %>
      </td>
      <td>編集する</td>
    </tr>
    <% end %>
  </tbody>
</table>